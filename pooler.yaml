apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: pooler-rw
  namespace: db
spec:
  cluster:
    name: cluster-0001
  type: rw                   
  instances: 2   
  pgbouncer:
    poolMode: session 
    parameters:
      max_client_conn: "1000"
      default_pool_size: "20"
